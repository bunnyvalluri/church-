import { translations } from '@/i18n';

export const eventManagerTranslations = {
  en: translations.en.eventManager,
  te: translations.te.eventManager,
  hi: translations.hi.eventManager,
};

export type EventManagerTranslationSchema = typeof translations.en.eventManager;
export default eventManagerTranslations;
