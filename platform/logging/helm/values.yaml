# Helm values file for KCM Logging Platform Umbrella Chart

global:
  environment: production
  clusterName: kcm-k8s-prod

namespace: logging

loki:
  replicas: 3
  retention: 30d
  storage:
    type: s3
    bucket: kcm-loki-chunks

alloy:
  enabled: true
  configMapName: alloy-config
