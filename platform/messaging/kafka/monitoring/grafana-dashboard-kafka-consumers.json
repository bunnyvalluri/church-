{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "Total Consumer Lag by Consumer Group",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [
        {
          "expr": "sum(kafka_consumergroup_lag) by (consumergroup)",
          "legendFormat": "{{consumergroup}}"
        }
      ]
    },
    {
      "title": "Consumer Lag per Topic",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "targets": [
        {
          "expr": "sum(kafka_consumergroup_lag) by (topic)",
          "legendFormat": "{{topic}}"
        }
      ]
    },
    {
      "title": "Consumer Fetch Latency (ms)",
      "type": "timeseries",
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "targets": [
        {
          "expr": "rate(kafka_consumer_fetch_latency_sum[5m]) / rate(kafka_consumer_fetch_latency_count[5m])",
          "legendFormat": "Fetch Latency ms"
        }
      ]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["kafka", "consumers", "kcm"],
  "time": { "from": "now-1h", "to": "now" },
  "title": "KCM Kafka Consumer Lag & Performance",
  "uid": "kcm-kafka-consumers-overview"
}
