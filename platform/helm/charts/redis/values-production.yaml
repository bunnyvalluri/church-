# Production Overrides for Redis
replicaCount: 3
master:
  persistence:
    size: 20Gi
    storageClass: longhorn-fast
  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: 2000m
      memory: 4Gi
sentinel:
  enabled: true
