apiVersion: v1
kind: Service
metadata:
  name: {{ include "loki.name" . }}
  labels:
    {{- include "loki.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3100
      targetPort: 3100
      name: http-metrics
  selector:
    app.kubernetes.io/name: {{ include "loki.name" . }}
