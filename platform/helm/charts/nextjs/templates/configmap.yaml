apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nextjs.fullname" . }}-config
  labels:
    {{- include "nextjs.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{- $key }}: {{ $value | quote }}
  {{- end }}
